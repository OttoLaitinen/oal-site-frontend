import { wrapRootElement as wrap } from "./root-wrapper"
import "@fontsource/quicksand/700.css" // Defaults to weight 400 with all styles included.
import "@fontsource/karla/400.css" // Defaults to weight 400 with all styles included.

export const wrapRootElement = wrap
